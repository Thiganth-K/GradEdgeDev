# Example environment variables for backend-node
# Set these in your deployment platform (Render) or copy to a local .env file

PORT=5005
MONGO_URI=mongodb://<username>:<password>@<host>:<port>/gradedgedev
NODE_ENV=production
